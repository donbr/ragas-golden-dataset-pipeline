# Hugging Face token for dataset uploads
HF_TOKEN=your_huggingface_token_here
HF_REPO=your-username/ragas-golden-dataset

# OpenAI API configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_ORGANIZATION=your_organization_id_here  # Optional

# LLM configuration
LLM_MODEL=gpt-4.1-mini
EMBEDDING_MODEL=text-embedding-3-small

# Project configuration
TESTSET_SIZE=10
DOCS_PATH=data/
OUTPUT_PATH=output/
KG_OUTPUT_PATH=output/kg.json

# Prefect settings (optional - for UI visualization only)
PREFECT_API_URL=http://localhost:4200/api
# Enable ephemeral mode to run without a server
PREFECT_SERVER_ALLOW_EPHEMERAL_MODE=True 